<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.programacaobrasil.atmempresaconsultoria">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".EmpresaActivity"
            android:label="@string/empresa_detalhes" />
        <activity
            android:name=".ServicoActivity"
            android:label="@string/servicos_detalhes" />

        <activity android:name=".ClienteActivity"
            android:label="@string/cliente_detalhes"/>

        <activity android:name=".ContatoActivity"
            android:label="@string/detalhe_contato"></activity>
    </application>

</manifest>